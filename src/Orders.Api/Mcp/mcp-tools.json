{
  "tools": [
    {
      "name": "get_resource",
      "description": "Retrieves a resource by its ID. Replace with your domain-specific tool.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string",
            "format": "uuid",
            "description": "The resource ID (UUID)."
          }
        },
        "required": ["id"]
      }
    },
    {
      "name": "create_resource",
      "description": "Creates a new resource. Replace with your domain-specific tool.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "The resource name."
          },
          "dataJson": {
            "type": "string",
            "description": "JSON payload with resource-specific fields."
          }
        },
        "required": ["name"]
      }
    }
  ]
}
